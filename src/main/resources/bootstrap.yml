spring:
   application:
     name:
       canteen-employee-service
   config:
     import: "optional:configserver:"
   cloud:
     config:
      uri: http://localhost:9092
      
      
      
     
      